"use strict";


var Promise = require("bluebird");


function incrementAsync(arg0, callback) {
  setImmediate(function () {
    callback(null, arg0 + 1);
  });
}


Promise.try(incrementAsync, 12).then(function (result) {
  console.log("Result: %d", result);
});
